apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: api-network-latency
  namespace: default
spec:
  action: delay
  mode: all
  duration: "60s"
  selector:
    labelSelectors:
      app: postgres # ALVO: BANCO DE DADOS
  delay:
    latency: "500ms"
    correlation: "0"
    jitter: "0ms"

